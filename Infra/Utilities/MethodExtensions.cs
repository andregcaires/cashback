namespace Utilities
{
    public class MethodExtensions
    {
        public static T ConvertTo(T1 source, T target) 
        {
            var result = Activator.CreateInstance(target);
            foreach () {

            }

            return (T) result;
        }
    }
}