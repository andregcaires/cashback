using System.Net.Http;
using System.Threading.Tasks;

namespace SpotifyService.Service
{
    public class SpotifyService
    {
        public async Task GetToken()
        {
            using (var http = new HttpClient())
            {
                http.
            };
        }
    }
}